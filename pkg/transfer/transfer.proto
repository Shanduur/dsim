syntax = "proto3";

package transfer;

option go_package = "pkg/transfer";

message Message {
  string Body = 1;
}

message Response {
  StatusCode Code = 2;
}

enum StatusCode {
  Unknown = 0;
  Ok = 1;
  Error = 2;
}

service TransferService {
  rpc SendPackage(Message) returns (Response) {}
}